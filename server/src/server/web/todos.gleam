import server/web
import wisp

pub fn todos_of_creator(
  req: wisp.Request,
  ctx: web.Context,
  id: String,
) -> wisp.Response {
  todo
}

pub fn todo_of_id(
  req: wisp.Request,
  ctx: web.Context,
  id: String,
) -> wisp.Response {
  todo
}
